args:
  # Test without MITM for now (will test replay with 0 transactions)
  deploy_mitm: false

  # Use op-geth with pessimistic consensus v0.8
  sequencer_type: op-geth
  consensus_contract_type: pessimistic
  aggkit_image: ghcr.io/agglayer/aggkit:0.8.0

  # Minimal additional services
  additional_services:
    - test_runner
