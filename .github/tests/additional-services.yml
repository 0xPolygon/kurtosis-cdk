args:
  verbosity: debug
  additional_services:
    - agglogger
    - arpeggio
    # - assertoor
    # - blockscout # The service experiences Out of Memory (OOM) termination when deployed inside github runners.
    # - blutgang
    - bridge_spammer
    - erpc
    - observability
    # - pless_zkevm_node # zkevm-node doesn't support fork12.
    - rpc_fuzzer
    - status_checker
    - test_runner
    - tx_spammer
