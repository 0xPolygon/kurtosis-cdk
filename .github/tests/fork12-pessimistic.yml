args:
  zkevm_contracts_image: leovct/zkevm-contracts:v9.0.0-rc.5-pp-fork.12
  additional_services: []
  consensus_contract_type: pessimistic
  sequencer_type: erigon
  erigon_strict_mode: false
  gas_token_enabled: false
  zkevm_use_real_verifier: false
  enable_normalcy: true
  agglayer_prover_sp1_key: ""
  # activate the `MockProver` for `agglayer-prover` as primary executor
  agglayer_prover_primary_prover: mock-prover
  # activate the `NetworkProver` for `agglayer-prover` as primary executor
  # agglayer_prover_primary_prover: network-prover
