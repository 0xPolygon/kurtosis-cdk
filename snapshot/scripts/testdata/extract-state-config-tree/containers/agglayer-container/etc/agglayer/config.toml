# K7 fixture: minimal stand-in for a captured /etc/agglayer/config.toml.
# Content is illustrative only -- extract-state_test.sh asserts on the FILE
# TREE SHAPE (which files land where), not on this file's contents.
[settlement.pessimistic-proof-tx-config]
confirmations = 1
settlement-policy = "SafeBlock"
